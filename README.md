# rlogin
this is a simple login program that uses rust. it reads a file in /home/username/.login and splits it and compares it agaist user input before running a processes specified in the parameters
